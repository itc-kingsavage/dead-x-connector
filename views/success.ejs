<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <header class="header">
            <div class="logo">
                <h1>💀 DEAD-X-BOT</h1>
                <p class="subtitle">Session Created</p>
            </div>
        </header>

        <main class="main-content">
            <div class="success-page">
                <div class="success-icon">✓</div>
                <h2>Session Created Successfully!</h2>
                
                <div class="session-card">
                    <div class="session-field">
                        <label>Session ID</label>
                        <div class="copy-box">
                            <input type="text" value="<%= sessionId %>" readonly id="sessionId">
                            <button class="btn btn-secondary" onclick="copySessionId()">Copy</button>
                        </div>
                    </div>

                    <div class="session-field">
                        <label>Phone Number</label>
                        <p><%= phoneNumber %></p>
                    </div>

                    <div class="session-field">
                        <label>Expires At</label>
                        <p><%= new Date(expiresAt).toLocaleString() %></p>
                    </div>
                </div>

                <div class="usage-guide">
                    <h3>🚀 How to Use This Session</h3>
                    <div class="step-guide">
                        <div class="guide-step">
                            <span class="step-number">1</span>
                            <div>
                                <h4>Deploy DEAD-X-BOT</h4>
                                <p>Deploy your main bot on Render, Heroku, or any platform</p>
                            </div>
                        </div>
                        <div class="guide-step">
                            <span class="step-number">2</span>
                            <div>
                                <h4>Set Environment Variable</h4>
                                <p>Add <code>SESSION_ID=<%= sessionId %></code> to your environment variables</p>
                            </div>
                        </div>
                        <div class="guide-step">
                            <span class="step-number">3</span>
                            <div>
                                <h4>Bot Connects Automatically</h4>
                                <p>Your bot will use this session to connect to WhatsApp - no QR scanning needed!</p>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="code-example">
                    <h4>Example Usage in Your Bot:</h4>
                    <pre><code>// Fetch session from API
const sessionId = process.env.SESSION_ID;
const response = await fetch(`https://your-scanner.onrender.com/session/${sessionId}`);
const { session } = await response.json();

// Use session data to initialize WhatsApp client
// Session is already authenticated - no QR needed!</code></pre>
                </div>

                <div class="button-group">
                    <a href="/scan" class="btn btn-primary">Generate Another Session</a>
                    <a href="/" class="btn btn-secondary">Go Home</a>
                </div>
            </div>
        </main>

        <footer class="footer">
            <p>&copy; 2024 DEAD-X-BOT</p>
        </footer>
    </div>

    <script>
        function copySessionId() {
            const input = document.getElementById('sessionId');
            input.select();
            document.execCommand('copy');
            
            const btn = event.target;
            const originalText = btn.textContent;
            btn.textContent = 'Copied!';
            btn.style.background = '#10b981';
            
            setTimeout(() => {
                btn.textContent = originalText;
                btn.style.background = '';
            }, 2000);
        }
    </script>
</body>
</html>
